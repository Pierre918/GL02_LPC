{
  "name": "gl02_lpc",
  "version": "1.0.0",
  "description": "Offer an interface for a recursive descent parser implemented in Javascript for reading and processing the CRU format. This format allows to describe a list of time slots where rooms and to describe how they are used. The files are in text format and should respect the following grammar.",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@caporal/core": "^2.0.7",
    "colors": "^1.4.0",
    "ical": "^0.8.0",
    "ical-generator": "^8.0.1",
    "vega": "^5.30.0",
    "vega-lite": "^5.21.0"
  }
}
